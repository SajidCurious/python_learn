# In Python, a set is an unordered collection of unique elements, which means each element appears only once. Sets are defined using the set() function or curly braces {}.

sam = {1,2,3,4,5,6}
print(sam)